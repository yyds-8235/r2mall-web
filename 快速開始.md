# 快速開始指南

## 📋 前置條件

確保您的開發環境已安裝：
- Node.js >= 16.x
- npm >= 7.x

## 🚀 啟動項目

### 1. 安裝依賴
```bash
npm install
```

### 2. 啟動開發服務器
```bash
npm run dev
```

項目將在 http://localhost:5173 運行

## 🔧 後端配置

### API 服務器配置
在 `vite.config.ts` 中已配置代理：
```typescript
proxy: {
  '/api': {
    target: 'http://localhost:8080', // 修改為您的後端服務地址
    changeOrigin: true,
    rewrite: (path) => path.replace(/^\/api/, '')
  }
}
```

### WebSocket 服務器配置
在 `src/utils/websocket.ts` 中修改：
```typescript
this.url = `ws://localhost:8082/im/${fromRole}/${toId}`;
```
將 `localhost:8082` 修改為您的 WebSocket 服務器地址。

## 📱 功能演示路徑

### 用户端
- 登錄/註冊: `/login`
- 首頁: `/user/home`
- 商品列表: `/user/products`
- 商品詳情: `/user/products/:id`
- 購物車: `/user/cart`
- 結算: `/user/checkout`
- 訂單列表: `/user/orders`
- 訂單詳情: `/user/orders/:orderNo`
- 個人中心: `/user/profile`

### 商家端
- 控制台: `/merchant/dashboard`
- 商品管理: `/merchant/products`
- 店鋪設置: `/merchant/profile`
- 客服消息: `/merchant/messages`

## 🧪 測試賬號

### 用戶端測試
1. 訪問 `/login`
2. 點擊「用戶註冊」標籤
3. 輸入用戶名和密碼進行註冊
4. 註冊成功後使用該賬號登錄

### 商家端測試
1. 訪問 `/login`
2. 點擊「商家入駐」標籤
3. 輸入商家號、店鋪名稱和密碼進行入駐
4. 入駐成功後使用該賬號登錄

## 📦 構建生產版本

```bash
npm run build
```

構建完成後，產物在 `dist` 目錄。

## 🔍 預覽生產版本

```bash
npm run preview
```

## 🛠️ 開發工具

### VSCode 推薦插件
- ESLint
- Prettier
- TypeScript Vue Plugin (Volar)

### 代碼規範檢查
```bash
npm run lint
```

## ⚠️ 注意事項

1. **圖片上傳功能**：當前圖片上傳僅接受 URL 輸入，如需實現真實文件上傳，需配置文件服務器。

2. **支付功能**：當前為模擬支付，實際支付需對接支付網關。

3. **WebSocket 連接**：確保 WebSocket 服務器正常運行，否則客服聊天功能無法使用。

4. **跨域問題**：開發環境已通過 Vite 代理解決，生產環境需在 Nginx 或後端配置 CORS。

## 📞 技術支持

如遇到問題，請檢查：
1. Node.js 版本是否符合要求
2. 依賴是否完整安裝
3. 後端服務是否正常運行
4. 瀏覽器控制台是否有錯誤信息

## 🎉 開始開發

現在您可以開始開發了！建議：
1. 先熟悉項目結構（查看 README.md）
2. 了解 API 接口定義
3. 查看現有組件和頁面的實現
4. 根據需求進行功能擴展

